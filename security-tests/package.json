{
  "name": "royal-clean-firestore-security-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-royal-clean --config ../firebase.emulator.json --only firestore,auth \"node --test --test-concurrency=1 firestore.test.mjs ../functions/test/backend.integration.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0",
    "firebase-tools": "^15.30.2"
  }
}
